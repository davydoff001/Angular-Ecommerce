import { DavShopValidators } from './dav-shop-validators';

describe('DavShopValidators', () => {
  it('should create an instance', () => {
    expect(new DavShopValidators()).toBeTruthy();
  });
});
